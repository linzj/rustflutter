# Copyright 2013 The Flutter Authors. All rights reserved.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.
#
# rustflutter M0 stub.
#
# lib/ui is Tier C: the C++ engine-object wrappers are present, but 73 of them
# still inherit RefCountedDartWrappable<T> / take Dart_Handle constructor
# arguments, so nothing here compiles yet. De-Dart-ifying them is M2 work; see
# PORTING_STATUS.md.
#
# The real build rules are preserved in BUILD.gn.upstream. This stub exists so
# that the //flutter/lib/ui label still resolves for the shell and embedder
# BUILD.gn files that reference it (they are Tier B and equally not built yet),
# without dragging unbuildable sources into the graph.

group("ui") {
}
