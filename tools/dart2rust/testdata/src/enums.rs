// Generated by tools/dart2rust from upstream `Axis`
// (Dart enum -> Rust enum).

#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum Axis {
    Horizontal,
    Vertical,
}

// Generated by tools/dart2rust from upstream `MainAxisAlignment`
// (Dart enum -> Rust enum).

#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum MainAxisAlignment {
    Start,
    End,
    Center,
    SpaceBetween,
    SpaceAround,
}

// Generated by tools/dart2rust from upstream `Season`
// (Dart enum -> Rust enum).

/// Enhanced: it has a method, so translating it as a plain enum would lose one.
// NOT TRANSLATED: `Season` is an enhanced enum -- a Rust
// enum plus an impl, which is a separate job.

// Generated by tools/dart2rust from upstream `Layout`.
//
// Translated, not ported: this is the compiler's output, not a
// hand-written re-expression. See tools/dart2rust/README.md.

#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Layout {
    pub axis: Axis,
    pub alignment: MainAxisAlignment,
}

impl Layout {
    pub const fn new(axis: Axis, alignment: MainAxisAlignment) -> Self {
        Self {
            axis: axis,
            alignment: alignment,
        }
    }

    pub fn is_horizontal(&self) -> bool {
        (self.axis == Axis::Horizontal)
    }

    /// Reads a value of another enum, so the two do not get confused.
    pub fn is_centred(&self) -> bool {
        (self.alignment == MainAxisAlignment::Center)
    }

    /// A multi-word value, where the renaming actually does something.
    pub fn is_spaced(&self) -> bool {
        (self.alignment == MainAxisAlignment::SpaceBetween)
    }
}
