use crate::dart_prelude::Object;
use crate::dart_prelude::*;
use crate::DartAny;
use crate::Type;

// Generated by tools/dart2rust from upstream `Rung`
// (abstract -> trait).

/// A trait to hold the reference. `implements` is not this compiler's shape
/// yet -- only `extends` produces an `impl` -- and upstream's version of this
/// is `Alignment extends AlignmentGeometry`, which is the same thing.

impl DartEq for dyn Rung {
    fn dart_eq(&self, other: &Self) -> bool {
        std::ptr::addr_eq(self as *const Self, other as *const Self)
    }
    fn dart_hash_code(&self) -> i64 {
        (self as *const Self as *const u8 as usize as i64) & 0x3fff_ffff
    }
}

pub trait Rung: DartAny + std::fmt::Debug {
    fn dart_self_rung(&self) -> std::rc::Rc<dyn Rung>;

    fn steps(&self) -> Result<i64, std::rc::Rc<dyn Object>>;
}
impl PartialEq for dyn Rung {
    fn eq(&self, other: &Self) -> bool {
        std::ptr::addr_eq(self, other)
    }
}
impl Eq for dyn Rung {}
impl std::hash::Hash for dyn Rung {
    fn hash<H: std::hash::Hasher>(&self, state: &mut H) {
        (self as *const Self as *const u8 as usize).hash(state)
    }
}

// Generated by tools/dart2rust from upstream `Ladder`.
//
// Translated, not ported: this is the compiler's output, not a
// hand-written re-expression. See tools/dart2rust/README.md.

#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Ladder {
    pub steps: i64,
}

impl Ladder {
    pub const fn new(steps: i64) -> Result<Self, std::rc::Rc<dyn Object>> {
        Ok({ Self { steps: steps } })
    }

    /// A plain counting loop.
    pub fn climbed(&self) -> Result<f64, std::rc::Rc<dyn Object>> {
        let mut total: f64 = 0.0;
        let mut i: i64 = 0;
        while (i < self.steps) {
            total = (total + (i as f64));
            i = (i + 1);
        }
        Ok(total)
    }

    /// A `while` with the same work, so the two lowerings can be compared: a
    /// `for` is a block holding the declaration with the update at the end of the
    /// body, which is exactly this.
    pub fn climbed_the_long_way(&self) -> Result<f64, std::rc::Rc<dyn Object>> {
        let mut total: f64 = 0.0;
        let mut i: i64 = 0;
        while (i < self.steps) {
            total = (total + (i as f64));
            i = (i + 1);
        }
        Ok(total)
    }

    /// Two updates, and a condition that stops before the declaration would.
    pub fn paired(&self) -> Result<f64, std::rc::Rc<dyn Object>> {
        let mut total: f64 = 0.0;
        let mut i: i64 = 0;
        let j: i64 = 10;
        while (i < j) {
            total = (total + 1.0);
            if (total > 3.0) {
                return Ok(total);
            }
            i = (i + 1);
        }
        Ok(total)
    }

    /// `identical(this, other)` -- the fast path at the top of an `operator ==`,
    /// which is where 140 of upstream's 259 live.
    ///
    /// The parameter is the abstract `Rung`, not `Ladder`, and that is not
    /// decoration: a `Ladder` parameter arrives by value, because a translated
    /// value type is `Copy`, and the address of a copy is not the address of
    /// anything the caller has. Only a reference can be asked this question, so
    /// only a reference is allowed to be.
    pub fn is_the(&self, other: std::rc::Rc<dyn Rung>) -> Result<bool, std::rc::Rc<dyn Object>> {
        Ok(std::ptr::eq(
            self as *const _ as *const (),
            std::rc::Rc::as_ptr(&other) as *const u8 as *const (),
        ))
    }

    // NOT TRANSLATED: Ladder.isTheCopy
    //   unsupported `identical` on something that is not a reference: other
    /// Refused, and it has to be: `other` is a `Ladder`, so it arrives by value.
    /// Comparing the address of a copy would compile and would always say false.
    pub fn is_the_copy(&self, other: Ladder) -> Result<bool, std::rc::Rc<dyn Object>> {
        panic!("dart2rust: not translated: unsupported `identical` on something that is not a reference: other")
    }
}

impl FromDynamic for Ladder {
    fn from_dynamic(value: &std::rc::Rc<dyn Object>) -> Option<Self> {
        value.dart_cast_any::<Self>()
    }
    fn from_same(value: &Self) -> Option<Self> {
        Some(value.clone())
    }
}

impl NativeAnswer for Ladder {
    fn from_answer(answer: std::rc::Rc<dyn Object>, symbol: &str) -> Self {
        match answer.dart_cast_any::<Self>() {
            Some(value) => value,
            None => panic!(
                "native `{}` answered {:?} where Ladder was declared",
                symbol, answer
            ),
        }
    }
    fn absent() -> Self {
        panic!("native answered nothing where Ladder was declared")
    }
}

impl DartNullable for Ladder {
    type Or = Option<Self>;
    fn option(or: Option<Self>) -> Option<Self> {
        or
    }
    fn from_option(option: Option<Self>) -> Option<Self> {
        option
    }
}

impl DartEq for Ladder {
    fn dart_eq(&self, other: &Self) -> bool {
        self == other
    }
}

impl DartAny for Ladder {
    fn dart_to_string(&self) -> String {
        format!("Instance of '{}'", "Ladder")
    }
    fn dart_eq_any(&self, other: &dyn std::any::Any) -> bool {
        match other.downcast_ref::<Self>() {
            Some(o) => self.dart_eq(o),
            None => false,
        }
    }
    fn dart_hash_any(&self) -> i64 {
        self.dart_hash_code()
    }
    fn dart_runtime_type(&self) -> Type {
        Type::of("Ladder")
    }
    fn dart_cast(&self, __t: std::any::TypeId) -> Option<std::boxed::Box<dyn std::any::Any>> {
        if __t == std::any::TypeId::of::<Self>()
            || __t == std::any::TypeId::of::<std::rc::Rc<Self>>()
        {
            return Some(std::boxed::Box::new(std::rc::Rc::new(self.clone())));
        }
        if __t == std::any::TypeId::of::<dyn Object>()
            || __t == std::any::TypeId::of::<std::rc::Rc<dyn Object>>()
        {
            return Some(std::boxed::Box::new(
                std::rc::Rc::new(self.clone()) as std::rc::Rc<dyn Object>
            ));
        }
        if __t == std::any::TypeId::of::<dyn Rung>()
            || __t == std::any::TypeId::of::<std::rc::Rc<dyn Rung>>()
        {
            return Some(std::boxed::Box::new(self.dart_self_rung()));
        }
        None
    }
}

impl Rung for Ladder {
    fn dart_self_rung(&self) -> std::rc::Rc<dyn Rung> {
        std::rc::Rc::new(self.clone())
    }

    fn steps(&self) -> Result<i64, std::rc::Rc<dyn Object>> {
        Ok(self.steps)
    }
}
