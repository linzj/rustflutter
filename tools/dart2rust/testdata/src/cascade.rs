use crate::DartAny;
use crate::Set;

// Generated by tools/dart2rust from upstream `Paint`.
//
// Translated, not ported: this is the compiler's output, not a
// hand-written re-expression. See tools/dart2rust/README.md.

#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Paint {
    /// Set at the declaration **and** never by the constructor.
    pub width: f64,
    pub alpha: f64,
    pub blur: f64,
}

impl Paint {
    pub fn new() -> Self {
        Self {
            width: 0.0,
            alpha: 0.0,
            blur: 0.0,
        }
    }

    pub fn widen(&mut self, by: f64) -> () {
        self.width = (self.width + by);
    }
}

impl DartAny for Paint {
    fn as_any(&self) -> &dyn std::any::Any {
        self
    }
}

// Generated by tools/dart2rust from upstream `Tinted`.
//
// Translated, not ported: this is the compiler's output, not a
// hand-written re-expression. See tools/dart2rust/README.md.

/// A field set at its declaration **and** by a constructor, so which one wins
/// is observable. Dart says the constructor does. Without a case having both,
/// the order is never tested and a mutation reversing it survives -- which it
/// did, the first time this fixture was written.
///
/// An initialiser list, not a constructor *body*: a body is refused, because it
/// used to be dropped silently and `Tinted(v)` came out ignoring `v` entirely.
#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Tinted {
    /// Both a declaration value and a constructor that overrides it.
    pub opacity: f64,
    /// Only a declaration value.
    pub tint: f64,
}

impl Tinted {
    pub fn new(value: f64) -> Self {
        Self {
            opacity: value,
            tint: 0.5,
        }
    }
}

impl DartAny for Tinted {
    fn as_any(&self) -> &dyn std::any::Any {
        self
    }
}

// Generated by tools/dart2rust from upstream `Painter`.
//
// Translated, not ported: this is the compiler's output, not a
// hand-written re-expression. See tools/dart2rust/README.md.

#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Painter {}

impl Painter {
    pub const fn new() -> Self {
        Self {}
    }

    /// One step.
    pub fn thin(&self) -> Paint {
        {
            let mut cascaded: Paint = Paint::new();
            cascaded.width = 1.0;
            cascaded
        }
    }

    /// Three steps, each a different field and value: dropping any one changes a
    /// different number.
    pub fn styled(&self) -> Paint {
        {
            let mut cascaded: Paint = Paint::new();
            cascaded.width = 2.0;
            cascaded.alpha = 3.0;
            cascaded.blur = 4.0;
            cascaded
        }
    }

    /// A method call among the writes, so the two kinds of step are both covered.
    pub fn widened(&self) -> Paint {
        {
            let mut cascaded: Paint = Paint::new();
            cascaded.width = 5.0;
            cascaded.widen(2.0);
            cascaded
        }
    }
}

impl DartAny for Painter {
    fn as_any(&self) -> &dyn std::any::Any {
        self
    }
}
