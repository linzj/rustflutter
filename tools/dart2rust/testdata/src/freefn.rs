pub fn halve(v: f64) -> f64 {
    (v / 2.0)
}

pub fn thrice(v: f64) -> f64 {
    (v * 3.0)
}

// Generated by tools/dart2rust from upstream `Gauge`.
//
// Translated, not ported: this is the compiler's output, not a
// hand-written re-expression. See tools/dart2rust/README.md.

#[derive(Clone, Copy, Debug, PartialEq)]
pub struct Gauge {
    pub value: f64,
}

impl Gauge {
    pub const fn new(value: f64) -> Self {
        Self { value: value }
    }

    pub fn reduced(&self) -> f64 {
        halve(self.value)
    }

    /// A top-level function calling another one, and a class calling both.
    pub fn amplified(&self) -> f64 {
        thrice(halve(self.value))
    }
}
