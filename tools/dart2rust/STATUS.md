## 1. 文档与总目标
- 目标已从“翻译得完”改成“**跑得起来**”：把 gallery 翻成 Rust，跑在上游 Flutter engine 的 AOT 模式里。
- 宿主路线选 **A**：Rust VM 顶掉 `libdart`，engine 不改；B 是改 engine 的退路，退时要明说。

## 2. 核心模型
- 异常一律走 `Result`：`throw = Err`，`rethrow = Err(e)`，`try = match`，`finally` 在分派前跑。
- 错误/对象手柄最终统一到 `Rc<dyn DartAny>`，删掉注册表，协议走 vtable。
- 对象模型：`Rc<RefCell<T>>`、回边 `Weak`、身份令牌；值类身份仍是最大欠账之一。

## 3. 尺子与当前读数
- 编译链：`stubs.py` 量 stub / refusal / 可达 crate / error。
- 夹具尺子：`bin/fx.sh` / `bin/allfx.sh`，判据是 **panicgate**（panic 一律不算过，先于退出码查）。2026-09-11 起缓存**按每一步自己的输入分档**（`bin/fx/fingerprint.sh`），不是一把大锁：`fp_prelude` = `lib/prelude.dart`（它只声明一个 `const rustPrelude`，只被逐字写成 `dart_prelude.rs`，所以改它不可能改动别的产物）；`fp_code` = 翻译器其余部分；`fp_sdk` = 夹具源码那一端的输入（SDK、platform dill、`package_config.json`），**dill 与 Dart 那一端只认它**——`gen_kernel --aot --tfa` 是每个夹具 5.85s（占翻译 10.6s 的 55%），而后端动不了它一个字节。缓存戳从前读在翻译**之后**，只省了 cargo 那 3.5s。三档实测（104 个夹具，3 并发）：**什么都没动** 6m51 → **11.6s**（判据逐条相同，底线是故意红的 `ffistruct`，它失败就不写戳，每遍全跑）；**只动 prelude** 每夹具 14s → 5.2s；**动后端** 每夹具 10.25s → **4.67s**，整遍 8m25 → **2m55**。墙钟数要看机器多热才有意义——同样 33 分钟 CPU 的一遍扫描，一次 8m25、一次 22m39（跟在 release 构建后，页缓存冷，并行度 ~4.5 核掉到 ~1.7 核）；**跨机器状态可比的是 CPU 时间：33m00 → 11m35**。
- 运行尺子：`dart_main` + `run_main.sh`，修启动路径第一个 panic。
- 渲染尺子：与 Flutter 参考遍历对账，参考 `ref_render_walk_settled.txt` **707 行**（707 行全部非空；`render_ruler.py` 的 `nodes=` 数的就是它，`typediff` 含长度差，所以 0 差异同时保证行数相同）。**曾写作 708，那是旧数**：重量于 2026-09-11 ws1094，参考文件与五次运行都是 707。
- **panic 尺子 `bin/panic_ruler.py`（ws1094 新增）**：把工作区里**每一种会中止的写法**数全——`panic!`/`unreachable!`/`todo!`/`assert*`/`.expect`/`.unwrap()`——**先出总数再分类**，对不上就报 MISCOUNTED。它取代的是 `grep -c 'panic!("uncaught'`：那是一行的模式，而 `erased_cast_failed` 的字符串写在 `panic!(` 的下一行，所以尺子报 0、东西还在、六处在调它。**量一个字符串只会静默地错，量总数不会。**
  - **`a8055b16` 发现它自己在发牌照**：它把任何认不出的 `dart2rust:` 消息折进一个桶 `dart2rust: .. (other)`，并把那个桶算作合法，于是宣布规则收尾的那天它的最后一行写着 0，真数是 **516**。前缀是这个编译器写的，前缀后面说的话却未必是这个编译器的事实——`panic!("dart2rust: a {} where a `{}` was wanted")` 就是一个挂着本编译器名字的 Dart `TypeError`。桶已删，每条消息各按原文单列；**新出现的一条在有人替它辩护之前一律算不合法**。
- **当前读数（ws1128）**：拒绝 **9**（ws1101 的 12 起算;ws1123 去掉 `Platform.toJson`,ws1124 去掉 `_addLicenses` ×2），桩 **14**（ws1098 的 36 起算，每轮 gone N / new 0，`stubdiff.sh` 逐字节；ws1127 → ws1128 gone 1：`_RouteEntry.handleDidPopNext`），unstubbable **0**，可达 **69**，0 error；渲染树连采五次 **707 节点 / 0 panic / 0 类型差异**；夹具 **129 个，128 AGREE + `ffistruct` 故意红**；panic 尺子 **「程序还能走、这边走不了」的中止点 = 0**（ws1121 的 `fold` 两处 `expect` 让它变 2,ws1122 改成不中止的写法后归 0）。
  - 510 → 0 的去向，按处置分：**真删掉 504 处**——494 处懒 `late` 尾读（`protocols.dart` 改成 `match`，返回刚算出的值就不必读回来）、5 处 `four()`/`eight()` 把数组 `try_into` 成它自己、1 处 Mutex 换成全函数的 `into_inner`、2 处 `Future.value()`/`Future.delayed` 对非空 T 取 null 改 `Err`、1 处 `FutureOr::Value` 空着（和十行下的 `then` 给同一答案）、1 处 `Sink::close` 补上错误通道；**1 处改成本来的措辞**（`dart:ffi` 没有该平台的 Abi 行是一条拒绝）；**5 处逐条辩上名单**，每条 1 个站点，理由写在尺子里。
  - 名单的规矩写进了 `bin/panic_ruler.py`：**一条消息覆盖很多站点就不该上名单**。494 处懒格子是一个发射点，答案是别再发射中止点，不是给它取个名字。
- 体积：ws1086 收下 `--icf=all` 后，release `.text` **56,617,250**，stripped **112,917,488**；从 work_size 起点 `.text` 75,158,528 → 56,617,250（−24.7%），stripped 135,833,000 → 112,917,488（−16.9%）；对官方 `libapp.so` 的 `.text` 从 6.11× 降到 **4.61×**。ws1094 把 670 处 `.unwrap()` 换成 `?`/`Err` 之后量：`.text` +61,632（**+0.11%**），stripped **−86,008**——work.md 第十一节那条“没量”的账，现在量了：**`?` 的传播代码没有想象中贵，`Err` 构造还替掉了一批 panic 的格式化字符串。**

## 4. 已闭合/已落地
- **ws1128 `handleDidPopNext` 清了**(夹具 `weakrefcovariant`,无改动红/有改动绿):字段是源码里写明的 `WeakReference<_RoutePlaceholder>`,存进去的是 `WeakReference<Route<dynamic>>(poppedRoute)`——Dart 在单元素包装上的协变。coerce 加一条 `WeakReference<A> → WeakReference<B>`,目标按元素规则转(这里是上转 trait 对象),后端拼 `.map_target(|v| ..)`(prelude 加的,上转不会失败);AOT 把 `WeakReference(..)` 去虚化成 `_WeakReference`,`normalName` 收成一个名字。ws1127 → ws1128 gone 1 / new 0。夹具路上撞见一个**静默错答案**没修、先记下:`NamedRoute` 用字段 `name` 覆盖了经具体基类 `Placeholder` 继承来的 getter,`impl Marker for NamedRoute` 的 `name` 转发到了 `Placeholder::name(self)`——读到基类的值。gallery 的 `_RoutePlaceholder` 没成员,不受影响。
- **ws1127 `pushNamed` 清了,`handleDidPopNext` 的闭包那一堵墙也过了**(夹具 `pushnamedthen`、`erasedcompleterthen`,都无改动红/有改动绿):`f.then(cb)` 的 `R` 是回调**体**交回来的东西,体里的 `T?` 是裸 `Option<T>`,而调用记的类型是嵌套投影 `<T as DartNullable>::Or`——turbofish(后端)与调用记的结果类型(前端)都改成体的拼法,方法返回 `Future<T?>` 处再由 `Future<A> → Future<B>` 映射用 `from_option` 收回投影。`_throughReceiver` 再放宽一步:接收者记的实参是**可空** `dynamic` 时(`Completer<T?>`、`T` 擦除),按 IR 名字代入(`_putIn`),kernel 层代 `Object?` 会掉 `?`;`.future` 记成 `Future<dynamic?>`,`_receiver` 把它映射进 kernel 看到的 `Future<dynamic>`,`then((dynamic result) ..)` 就对了。ws1126 → ws1127 gone 1 / new 0(`push_named`);`handle_did_pop_next` 闭包不再报错,换成了下一句:TFA 把字段 `lastAnnouncedPoppedNextRoute` 收窄成 `WeakReference<_RoutePlaceholder>`,而这里造的是 `WeakReference<Route>`——prelude 泛型里的协变,同 `Equality<E?>` 那一族。
- **ws1126 `scheduleTask` 那两条清了,STATUS 那句「先把 IrType 打出来看」是对的**(夹具 `erasedfuturefield`,无改动红/有改动绿):`DART2RUST_TRACE_COERCE=Future` 显示 `return entry.completer.future` 根本没进 coerce——值没记类型。`_throughReceiver` 原来只认「声明类型**就是**类的类型参数」(`item.tween`),`Completer<T>.future` 声明的是 `Future<T>`,只是**提到** `T`;现在提到的也算:接收者把该参数记成 `dynamic` 时,把声明类型代入后按嵌套拼法记下(`DartFuture<Rc<dyn DartAny>>`),返回处的 `Future<A> → Future<B>` 映射再用 `dart_from_dynamic::<T>` 收回来。第一次量 gone 2 / new 1:`SemanticsNode.sendEvent` 里 `BasicMessageChannel<dynamic>.send` 声明 `Future<T?>`,kernel 把 `T?` 在 `T := dynamic` 下折成裸 `dynamic`,而 prelude 的 `Or` 是 `Option`——`_topBound` 那条本来就拼对了;声明里**可空地**提到参数的不走新规则(`_mentionsNullably`),第二次量 gone 2 / new 0(ws1124 → ws1126)。
- **ws1124 试过一次 ws1115 的 (a),撤了,原因是量出来的**:把类的擦除参数 `T?`(bound 为 `Object?`)拼成裸 `Rc<dyn DartAny>`(与方法参数那一支、与 kernel 一致),20 行的夹具就在三处自相矛盾——构造器把 `value` 塞进字段时按 kernel 的可空性包了 `Some`,`Completer.complete(value)` 的可选参数槽仍要 `Option`,`box.value == null` 的读记的类型仍是可空的(拼成 `.is_none()`)。「这个槽是不是 Option」在这些地方各自问的是 kernel 的 `?` 而不是 Rust 拼法;要走这条路得先把 `_acrossEdge`/`_widened`/可选参数包装/字段读的类型统一到问拼法,不是 `_type` 一处的事。
- **ws1124 `gzip.decode` 进了 prelude,`Isolate.run` 在本地跑**(夹具 `gzipinflate`,无改动红/有改动绿;两段 Python 生成的 gzip 负载——短的走固定 Huffman 块、长的走动态块——和一段 zlib 帧逐字节对上):`WidgetsFlutterBinding._addLicenses` 用 `compute(gzip.decode, bytes)` 解 `NOTICES.Z`,`const GZipCodec()` 是跨文件 const。prelude 加 `GZipCodec`/`ZLibCodec`(常量的七个字段照抄成结构体字面量)与完整的 RFC 1951 inflater(stored/fixed/dynamic 三种块)套 gzip/zlib 头(按魔数自动辨认,校验和不核),`Isolate::run` 把 computation 当场跑掉——这边只有一个 isolate,web 上的 `compute` 也是这样;`GZipCodec.decode`/`ZLibCodec.decode` 与 `utf8.decode` 一样按接收者算可失败(FormatException)。拒绝 11 → **9**,桩集合逐字节不变。
- **ws1123 `const JsonEncoder.withIndent('  ')` 进了 prelude 的常量表**(夹具 `jsonindent`,无改动红/有改动绿,整个缩进版式逐字符对上 Dart):`Platform.toJson`(package:platform)用它写自己,跨文件 const 实例除非 prelude 认识就拒绝。prelude 加 `JsonEncoder { indent }` 与 `convert`(紧凑 JSON 再按 Dart 的 `withIndent` 版式排:有成员的对象/数组每成员一行、空的保持 `{}`/`[]`、键后 `: `),`_constInstance` 表里按常量的 `indent` 字段拼结构体字面量。拒绝 12 → **11**,桩集合逐字节不变(gone 0 / new 0)。
- **ws1121 prelude 的 Stream 活起来了,有了 `StreamController`**(夹具 `streamcontroller`,无改动红/有改动绿):原来的 Stream 只装事先知道的事件;现在带 listeners 与 done,`StreamController::new(onListen, ..)` 在第一次 `listen` 后跑 `onListen`(`async` 闭包由 `DartFuture::spawn_named` 排进调度器,下一趟跑),`add` 直接送到 listener,`add_stream` 转发到源 done,`close` 发 onDone;`fold`/`to_list`/`first` 等到 done 再结。递送在调用者栈上,与 ready stream 一贯的偏差一致(Dart 全部异步递送;夹具用 `sync: true` 对齐,`onListen` 里加的事件 Dart 仍会压后,所以观察的是 listen 之后加的)。`LicenseRegistry.licenses` 与 `_PackagesViewState` 的 `fold` 2 桩没了(ws1120 → ws1121 gone 2 / new 0)。
- **ws1120 擦除孪生的实参按孪生自己的槽做 coerce**(夹具 `erasedtaskcallback`,无改动红/有改动绿):`SchedulerBinding.instance.scheduleTask<List<LicenseParagraph>>(license.paragraphs.toList, ..)` 的接收者是 trait 对象,走 `schedule_task__erased`,它的回调槽是 `Fn() -> FutureOr<Rc<dyn DartAny>>`,而前端把撕出来的方法按调用自己的 `T` 适配成了 `Fn() -> FutureOr<Vec<..>>`。后端四处 `__erased` 调用点的实参,凡参数类型提到方法自己的类型参数,就按 `T := Object` 的槽再 coerce 一次(`_erasedArgs`);`FutureOr<A>` 进 `FutureOr<B>` 补一条逐 case 映射(`IrMapElements` 的 `FutureOr` 臂)。`_PackageLicensePageState._initLicenses` 1 桩没了(ws1119 → ws1120 gone 1 / new 0)。夹具路上记两笔欠账没修:Rust 的 `async` 体不 poll 不跑,而 Dart 跑到第一个 `await`;`Future<T>.value(r)` 的 `r` 是 `FutureOr<T>` 时被当成 `T?` 拼了。
- **ws1119 投影类型带实参的去投影拼法**(夹具 `widermapfield`,无改动红/有改动绿):`ShortcutMapProperty extends DiagnosticsProperty<Map<ShortcutActivator, Intent>>`,在 `DiagnosticsProperty<Object?>` 的宽 impl 里读回字段 `_value`,`_plain` 造 `IrNullableOf` 只带了名字,拼成 `<Map<Rc<dyn DartAny>, Rc<dyn DartAny>> as DartNullable>::option(__v)`。`IrNullableOf.spelled` 带上整个类型(经 base 绑定代换)。1 桩没了(ws1118 → ws1119 gone 1 / new 0)。夹具路上碰到两堵已记录的墙,都绕开而不是修:无字段的值 struct 派生 `PartialEq` 让两个 `Key()` 相等(TFA 把没读过的字段删了),以及 `Prop<Map>` 直接流进 `Prop<Object?>` 槽会把 `T` 擦除。
- **ws1118 `void` 方法里 `return switch (event) { X() => cb?.call(event), .., _ => null }`**(夹具 `switchnullarm`,无改动红/有改动绿):`void?` 就是 `void`(`_normal`),但 null-aware 调用拼出来的是 `map(..).transpose()?` = `Option<()>`,IR 却记成 `()`;switch 表达式各臂共用一个未标类型的临时,AOT 折掉的那一臂(全程序没人设的回调)已经是 `()`,两臂不合。后端 `_nullAware` 对 unit 体加一层 `{ ..; }`。`RenderPointerListener.handleEvent` 1 桩没了。
- **ws1118 基类构造器初始化式内联进子类时,节点重建丢了 `rustType`,静态调用的类型实参也没代换**(夹具 `erasedstartlistening` 钉 `_install::<T>` 那一半;`Some` 那一半靠 gallery:`DART2RUST_TRACE_SOME=1` 全 gallery 只有一处闭包进 `Some(..)` 没类型,就是 `ChangeNotifierProvider.value`)。`flattening.dart` 的 `_substitute` 现在把原节点的类型(经 base 绑定代换)带到重建节点上,`IrStaticCall.typeArguments`/`IrNew.type` 一并代换,`IrNew.implementation` 不再丢。rustc 层面五行 `somerc.rs` 证明 `Some({..Rc::new(closure)}).clone()` 不能 unsize、带 `let __f: Rc<dyn Fn>` 就行。1 桩没了。
- **ws1118 带来的一条新桩当场清掉**:`Color(int value) : this._fromARGBC(value >> 24, ..)` 是 `const` 构造器,重定向实参带上类型后移位变成 `dart_shr(..)?`,`const fn` 里不能有 `?`(E0493)。字面量计数 < 64 的移位两边语义一致、不可能失败,保留运算符(夹具 `constshift`)。ws1117 → ws1118 对 ws1116 逐字节 gone 2 / new 0。
- **ws1116 `this.child!.hitTest` 撕出来的值持有句柄了**（夹具 `tearoffnullcheck`，无改动红/有改动绿）。撕方法的闭包在计数类里持句柄，判「根在 this」只看字段读链，`!` 一夹就落空——既不持有也不先绑定，闭包借了 `this_`（抽象类的体是自由 super 函数，`this_: &dyn`）再装进 `'static` 的 `Rc<dyn Fn>`：「lifetime may not live long enough」。`_rootedAtThis` 看穿 `NullCheck` 一条，1 桩没了。
- **ws1116 值类型的 `this` 上转成 trait 句柄走 `dart_object`**（夹具 `valuethishandle`）：mixin 里 `Holder(this, ..)` 落到值 struct 时 `_thisHandle()` 是 null，原来拼成 `(*self)` 传进要 `Rc<dyn Shape>` 的槽（E0308）。1 桩没了。
- Goal 2 无头引擎：能 build、layout、paint 出真帧。
- Goal 3 渲染树：节点类型、顺序、层级与参考一致，**707/707，忽略 `size=`/`offset=` 后 0 差异**。
- 剩余 508 行只差 `size=`：32 个 `RenderParagraph` 量成 `Size(0,0)`，因无头运行时不答 `Paragraph::*` native，且 native 桥不传接收者。**这是运行时缺口，不是翻译缺口。**
- `work.md`（2026-09-11 12:0x 版）七步全部落地：生成侧 `.unwrap()` 29,439 → 1,165 → **0**；最后留下的 495 处是懒初始化刚写完那一格的读，已改写成 `.expect("dart2rust: ..")`，把理由写进产物本身。prelude 的中止点 41 → 25 → **19**。手段是七条发射点上的通则（详见 ws1094 的提交说明），不是一处处包 `try`。
- **ws1094 那句“没有一处再说‘那个程序’的事”是错的**，错在尺子而不在手段：见上面 panic 尺子那条。真数是 516；ws1096 把 `dart_from_dynamic`（128 处调用）、`dart_function_of` 的三个提问者、`json_write` 的兜底和三处 Completer 改成值之后是 **510**，ws1098 把剩下的处置完是 **0**。顺手补上一个自己挖的坑：`DartFuture::map` 的回调槽是 `Fn(T) -> U`，于是 `dart_from_dynamic` 在它体内没有 `Result` 可走，冒出 4 个**匿名** `.unwrap()`——比有名字的 panic 更坏。改在槽上：回调可失败，它的错误就是这个 future 的错误，正是 Dart 对会抛的 `then` 的做法。
- Object 协议第二版最终在 ws1005/ws1006 做成：删 `dart_register`，四张表归零。
- 体积优化落地：常量表、`--icf=all`、`force-unwind-tables=no` 等。

## 5. 最近撤回与主要欠账
- ws1088 D：擦除降体积四刀，桩 655 / 139 / 79 / 57，可达 39 / 67 / 66 / 66，未达基线 36 / 69，撤回。
- ws1089 B：`DartStr = Cow<'static, str>` 改造走了一大半，夹具 60 → 64 → 70 → 71，但 gallery 尾巴未开始，整轮退回。
- ws1087 量清：D 的真实上限 **3.07 MB（5.4%）**；B 的 l10n **19,437,406 字节，占函数符号 28.8%**。
- 最大挡路工程：**值语义 / 别名 / counted**。11 条拒绝同一决定；做它曾量到 **+901 桩**。
- **14 条桩,逐条写下理由(ws1128;ws1126 的 16 条去掉 `pushNamed`、`handleDidPopNext`;ws1119 的 21 条去掉 `_initLicenses`、`LicenseRegistry.licenses`、`_PackagesViewState.licenses`、`scheduleTask` ×2)**。按「那个程序」的理由分:
  - **同一个 List 既是源又是目标,Rust 不能同时可变借两次,3 条**:`_mergeSort(list, compare, start, middle, list, firstTarget)`(`merge_sort`、`_merge_sort`);`ListBase.removeRange` 关缝时 `setRange(start, length - size, this, end)` 把自己交给自己的 `setRange`(`HashSink._iterate` 的 `_pendingData.removeRange`)。是程序里的别名,和「值语义 / 别名 / counted」一堵墙。
  - **Dart 的类型实参协变、Rust 的 trait 对象不协变,5 条**:`IterableEquality<E>` 把 `Equality<E>` 存进 `Equality<E?>` 字段(`equality.rs new`);`binarySearch<T extends Comparable<Object>>` 对 `T` 调 `compareTo(T)`、`DataTableDemo._sort` 把自家类当 `Comparable<Object?>` 用(cast 表只登记了 `Comparable<Self>`,ws1102 量过,声明成 `DartAny` 子 trait 会运行时抛);`RestorableEnum<T extends Enum>` 作为 `RestorableProperty<Object?>` 注册(`restore_state` ×2)——宽 impl 必须写在命名 `T` 的 crate 里,而孤儿规则两边都不许写。
  - **dart:ffi 的 `external`,2 条**(`widgets_window_win32.rs allocate`/`new`):同 4 条拒绝,kernel 里没有 Dart 体。
  - **本编译器的欠账,4 条,不是程序的理由(ws1128)**:dart:io 的 `HttpClient`/`Stream.pipe` 不在 prelude(`io_client send`);`TypedData` 在 prelude 是空 struct,而 `Uint8Buffer._buffer` 是 `Vec<i64>`,一个 `List<int>` 字段装着 Uint8List 时元素宽度已经丢了,`_buffer as TypedData` 无处落(`typed_buffer` ×2);槽位 mixin 三处改动要一起做(`slotted…debugDescribeChildren`)。
- **9 条拒绝(ws1124;ws1101 的 12 条去掉 `Platform.toJson` 的 `JsonEncoder` 与 `_addLicenses` ×2,另补上当时漏记的 `GetQueue._check`)**。4 个机制;4 条有关于「那个程序」的理由(dart:ffi 的 `external`),其余 5 条是本编译器的欠账:`identical` 作用在非引用上 ×2、`super.==`/`hashCode` ×2(ws1106 量过,值实现者会现造句柄)、`GetQueue._check` 里 `item.completer.completeError(e)` 是对 `dynamic` 接收者的调用(queue 包把 completer 存成 `dynamic`)。
- **12 条拒绝,逐条查过(ws1101)**。它们是 5 个机制,不是 12 件事;只有 4 条有一句关于「那个程序」的理由,其余 8 条是本编译器的欠账。
  - **dart:ffi / win32 windowing,4 条**(`_Win32PlatformInterface.initializeWindowing`、`_WindowingInitRequest.onMessage`、`_CallocAllocator.new`、`_CallocAllocator._fillMemory`)。四条都落在 dart:ffi 的 `external` 上:`_ffiCall` 是 AOT 编译器生成的本地跳板,`_createNativeCallableIsolateLocal` 带 `vm:external-name "Ffi_createNativeCallableIsolateLocal"`,`Uint8Pointer.operator[]=` 走到 `_storeUint8`——**kernel 里没有 Dart 体可译**,写一个体不是翻译这个程序,是在 prelude 里重做一遍 dart:ffi 运行时。外面那层也不是这个平台的:`_CallocAllocator()` 第一句 `DynamicLibrary.open('ole32.dll')`,`WindowingOwnerWin32()` 第二句 `if (!Platform.isWindows) throw UnsupportedError('Only available on the Win32 platform')`。`DART2RUST_OS=android` 下 Dart 自己也走不过去。这 4 条**留下**。
  - 跨文件 `const` 实例,**3 条**(`GZipCodec` ×2、`JsonEncoder`)。本编译器的欠账:一个 `const X(..)` 的类不在本文件时没被降下来。
  - `identical` 作用在非引用上,**2 条**(`VerticalCaretMovementRun.isValid`、`PageTransitionsTheme operator ==`)。欠账,并且和「值语义 / 别名 / counted」是同一件事:`Map`/`List` 在这边是值,没有引用同一性可比。
  - `super.==` / `super.hashCode` 落到 `Object`,**2 条**(`widgets_framework`)。**ws1106 试过一次,走死了,原因现在是量出来的**:`Object` 的这两个成员就是身份,prelude 的 `dart_identical_any` / `dart_identity_hash_code` 也都在,把 `_superCall` 里 `base == 'Object'` 的这两支路由过去、用 `_thisHandle()` 取自身句柄,`Widget.==` 确实能译出来。但 super 函数是抽象类**共享**的一份,而 `dart_self_X()` 对非 counted 的实现者是 `std::rc::Rc::new(self.clone())`——**现造的分配**。实测(一个 `abstract Node` + 值结构实现者):同一个对象的 `super == other` 答 `false`,Dart 答 `true`;两个字段相同的不同对象答 `true`,Dart 答 `false`。那是把一个看得见的拒绝换成静默的错答案,已撤回。
    - 要真做:要么让 `dart_self_X()` 对没有 `DartSelf` 的实现者拒绝而不是伪造,要么把身份比较发射在**每个实现者自己的**转发里(那里知道 `counted`),而不是共享的 super 函数里。
  - 动态槽上的 `completeError`,**1 条**(`get_below/queue_get_queue`)。欠账:`DynamicInvocation` 这一族本身没翻译。
- **`scheduler_binding.scheduleTask` 那两条(擦除类持有 `Completer<T>`)——诊断是错的,两次链都量过**。`_TaskEntry<T>` 被擦除,`entry.completer` 的类型已经是对的(`_erasedRead`,`expressions.dart`);我以为差的是**第二次读**(`.future`)按调用点的 `T` 定型。按这个改了两次:
  - 改全局 `_staticType`(对 `InstanceGet` 返回擦除后的类型):**29 → 1297 桩**。那里是促进、cast 和每一个分派问题的共同入口。
  - 只改 `_memberRustType` 里接收者那一处:**29 → 38,gone 0 / new 9**,一条都没修好。
  所以问题不在接收者定型。下次动这一族之前,先把 `entry.completer.future` 实际的 IrType 打出来看,而不是推。
  - **ws1115 `_RouteEntry.handleDidPopNext` 是同一堵墙,这次用 `DART2RUST_TRACE_ARG=Future` 看准了**:`then[0]` 的 `declared=Function(Future.T%)`、`param=Function(dynamic)`、`slotIr=null`。`Route<T>._disposeCompleter` 是 `Completer<T?>`,`T` 被擦除;字段拼成 `Completer<Option<Rc<dyn DartAny>>>`(擦除分支给 `dynamic?`,嵌套位置的可空 dynamic 打成 `Option`),而调用点 kernel 把 `T?` 在 `T := dynamic` 下坍成 `DynamicType`,`?` 从此没有任何载体——接收者 IrType、`_dispatchReceiverType`、`slotIr` 全是裸 `dynamic`。所以**孤例是字段那一种拼法**,不是用点;两种修法:(a) 嵌套位置的可空 `dynamic` 也打成裸 `Rc<dyn DartAny>`(与 kernel 一致,`null` 走 `Null` 对象),这是 backend `type()` 的一条改动,影响面是所有 `X<T?>`、`T` 被擦除的字段——要整条链量;(b) 让调用点从接收者**字段声明**取回 `Option`,即 scheduler 那条失败过的方向。试了 `_projectedSlot` 对擦除参数返回 true:在方法自带 `T` 的夹具上拼出 `<T as DartNullable>::Or`(错的 `T`),在 `Slot<dynamic>` 的夹具上不触发(`wanted` 已是 `DynamicType`),已撤。
- **`SlottedContainerRenderObjectMixin.debugDescribeChildren`——三次量过,别再按「把应用参数映射回去」这条路走**。局部声明成了具体的 `_ChipSlot`,而 super 函数是泛型的 `SlotType`;`_appliedBackMap` 只收**被擦除**的参数(`slots.dart:581`),保留参数的应用实参就留在体里了。
  - 去掉那个过滤:**gone 1 / new 1**——`debugDescribeChildren` 清了,`debugNameForSlot` 破了,因为 `slot is Enum ? slot.name : slot.toString()` 里 `enum_name_get_name` 要 `E: DartEnum`,而 `mixin SlottedContainerRenderObjectMixin<SlotType, ChildType extends RenderObject>` 的 `SlotType` 没有界。
  - 再给 mixin 参数按「每个应用都放枚举」加 `DartEnum` 界(`enumParameters` 已有这套,只是只看 Dart 声明的界):**28 → 31,gone 1 / new 4**。`insert/move/removeRenderObjectChild` 三个也跟着破,而且 `slot is Enum` 的**类型提升**在裸类型参数上仍然发射 `slot as Rc<dyn DartAny>`(E0605,`as` 不能用在类型参数上)。
  - 结论:这一条至少要三处一起改(应用参数映射回、参数的枚举界、类型参数的提升不走 `as`),而且第三处是通用的。不要再单独动前两处。
- 桩尾已是长尾：最大簇也只有几个。剩下多要子系统：`StreamController`、HTTP 客户端、gzip/JSON、ffi 写路径、`ListBase`、擦除类型参数界、tear-off coercion。

## 6. 下一步优先级
1. **别名/counted + `List<T>` 表示**：`Rc<RefCell<Vec<T>>>` 是表示，`Rc<dyn DartList<T>>` 是槽拼法；先做普查。
2. prelude 接口成员经对象调用：`Comparable`、`DartIterator` 等经 `dart_cast_to`。
   - **ws1102 量过一次,是个陷阱**。把 `Comparable<T>` 声明成 `DartAny` 的子 trait,`dart_cast_to` 就有了(`DartCastExt` 是对 `DartAny` 的 blanket impl),`DataTableDemo._sort` 也编译得过——但**运行时抛** `TypeError: type is not a subtype of type 'Comparable' in type cast`。原因:`class Score implements Comparable<Score>` 的 cast 表只登记了 `Comparable<Score>`,而代码要的是 `Comparable<Object?>`;Dart 靠协变直接过。
   - 试过的形状(Dart 答案 `-3|3|-1|1|s1,s2,s3`):`int compareCells(Object? a, Object? b) => (a! as Comparable<Object?>).compareTo(b);`,分别喂自家类、`int`、`String`,再用它做一次 `rows.sort`。
   - 所以这一条**不是**加个 supertrait 能了的:要让对象为**更宽的实例化**答话——为每个实现 prelude 泛型接口的类再发一个擦除实例化的 impl 并登记进 `dart_cast`。单加 supertrait 是把一个看得见的桩换成一个 Dart 不会抛的运行时错,按「任何 panic 都不算过」更糟,已撤回。
3. `Sink<T>` 进 `_preludeInterfaces`，补 `DartSink` 转发 impl。
4. 增强枚举每变体状态。
5. 运行期补：Paragraph native、Stream、HTTP、codec、ffi 回调。

**不做**：nightly 并行前端；按 SCC 拆 crate；翻译 `dart:core`。

## 7. 九条要记住的
零拒绝 ≠ 翻译好；`dart:core`/`dart:ui` 手写 prelude；仪器各有盲区；短路语义易静默错；夹具值要能分辨错配；变异要查断言对象；探针先查排序再查条件；拒绝升高可能是尺子变准；正则先看一条匹配；变异必须还编得过；数字必须带条件。

**一句话**：翻译与渲染结构已接近收尾，数字停在 **12 拒绝 / 34 桩 / 69 可达 / 707 行 0 差异 / 0 个"程序还能走"的中止点**；12 条拒绝已逐条查过，4 条留下并写明理由，8 条是欠账；再往下主要是运行时子系统与值语义/别名工程。
